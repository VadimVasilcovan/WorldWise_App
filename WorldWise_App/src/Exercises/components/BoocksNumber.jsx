/* import React from 'react';

export default function BooksNumber({ book }) {
  const numberOfBooks = book.length  // Safely access length, default to 0 if undefined

  return (
    <div>
      <h1>{numberOfBooks}</h1>
    </div>
  )
}
A */